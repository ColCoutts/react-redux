import { createStore } from 'redux';

const initialState = {
  id: null,
  body: null
};

function reducer(state = initialState, action) {
  switch(action.type) {
    
  }
}


const store = createStore();
